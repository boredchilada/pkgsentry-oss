ecosystem = "pypi"
name = "libfoo-native"
version = "2.1.0"
label = "good"
expected_verdict = "clean"
expect_rules = ["malware.env_bulk_exfil"]
forbid_rules = ["malware.env_exfil_tainted"]
notes = "FP guard for the env taint-gate: a native wrapper reads the build environment AND makes an unrelated HTTP call, but no env value flows into the send. env_bulk_exfil fires as a low-weight corroborating `medium` so the package stays clean; it must NOT escalate to the critical taint-confirmed rule (the native-wrapper false-malicious class)."
provenance = "synthetic"
